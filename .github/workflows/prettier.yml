name: Prettier
on:
  push:
    branches: [main]

  workflow_dispatch:

jobs:
  Prettier:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: actionsx/prettier
        uses: actionsx/prettier@v2
        with:
          args: --check .
